name: mini_quiz
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_tts: ^3.8.5
  logger: ^2.0.2
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/quizzes/quiz1.json

  fonts:
   - family: NotoSans
     fonts:
      - asset: fonts/NotoSansJP-VariableFont_wght.ttf
        style: normal
